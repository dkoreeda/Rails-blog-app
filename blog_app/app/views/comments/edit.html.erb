
<%= form_for @comment do |f| %>
  <% f.label :description %>
  <%= f.text_area :description, placeholder: :description, cols: 40, rows: 10 %>
  <%= f.hidden_field :post_id %> <%# change here, you were passing in :post_id which converts it to a string %>

  <%= f.submit %>
<% end %>
